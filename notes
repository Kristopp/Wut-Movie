     <div className="carousel-inner" role="listbox">
          {weekelyTrending.map((movies) => (
            <div className="carousel-item active">
              <img
                className="d-block img-fluid"
                src={`https://image.tmdb.org/t/p/w500/${movies.poster_path}`}
                alt="First slide"
              />
            </div>
          ))}